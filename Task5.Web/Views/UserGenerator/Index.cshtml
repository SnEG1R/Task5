<link rel="stylesheet" href="css/user-generator/user-generator.css">

<div class="text-light">
    <div class="header bg-dark w-100 d-flex justify-content-center align-items-center" style="height: 120px">
        <div class="block-region d-flex justify-content-center align-items-center flex-column">
            <span>Region</span>
            <select class="form-select" id="select">
                <option value="en">English</option>
                <option value="uk">Ukrainian</option>
                <option value="pl">Polish</option>
            </select>
        </div>
        <div class="block-range d-flex justify-content-center align-items-center flex-column">
            <label for="customRange" class="form-label">
                <span>Range error</span>
            </label>
            <input type="range" class="form-range" value="0" min="0" max="10" step="0.5" id="customRange">
            <input type="number" min="0" max="1000" value="0" id="input-customRange">
        </div>
        <div class="block-range d-flex justify-content-center align-items-center flex-column">
            <span>Seed</span>
            <input type="number" min="0" max="1000" value="0" id="input-seed">
            <button type="button" class="btn btn-danger mt-2 w-100 generate-seed">Generate</button>
        </div>
    </div>
    <div class="table-container">
        <table class="table table-dark table-striped">
            <thead>
            <tr>
                <th>№</th>
                <th>Random id</th>
                <th>Full name</th>
                <th>Address</th>
                <th>Phone number</th>
            </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

<script src="js/user-generator/sender.data.js"></script>
<script src="js/user-generator/seed.control.js"></script>
<script>
let slider = document.getElementById('customRange');
let input = document.getElementById('input-customRange');

select.addEventListener('click',  async () => {
    await GetPersonData(true);
});
slider.addEventListener('change', () => {
    input.value = slider.value;
});
input.addEventListener('change',  async () => {
    slider.value = input.value;
});
</script>